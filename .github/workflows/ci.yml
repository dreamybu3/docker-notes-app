name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Исправленная команда здесь
      - name: Run tests
        run: docker compose -f docker-compose.test.yml up --build --abort-on-container-exit